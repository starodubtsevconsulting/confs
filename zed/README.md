# Zed (home install)

Installs the Zed editor using the official install script from zed.dev.

Run:
```bash
./zed/setup.sh
```

Notes:
- Uses the upstream installer script.
- Ensures `~/.local/bin` is on PATH for `zed`.
